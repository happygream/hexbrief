name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      draft:
        description: 'Publish as draft release'
        required: false
        default: 'true'
        type: boolean

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            platform: win
            artifact: dist/*.exe
          - os: macos-latest
            platform: mac
            artifact: dist/*.dmg
          - os: ubuntu-latest
            platform: linux
            artifact: |
              dist/*.AppImage
              dist/*.deb

    runs-on: ${{ matrix.os }}
    name: Build ${{ matrix.platform }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      # Linux deps for Electron
      - name: Install Linux build dependencies
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libgtk-3-dev \
            libwebkit2gtk-4.1-dev \
            libappindicator3-dev \
            librsvg2-dev \
            patchelf \
            rpm

      # macOS — allow unsigned builds for now
      - name: Set macOS environment
        if: matrix.os == 'macos-latest'
        run: echo "CSC_IDENTITY_AUTO_DISCOVERY=false" >> $GITHUB_ENV

      - name: Build Next.js (static export)
        run: npm run build

      - name: Package Electron (${{ matrix.platform }})
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CSC_IDENTITY_AUTO_DISCOVERY: false
        run: npm run electron:build:${{ matrix.platform }}

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: hexbrief-${{ matrix.platform }}
          path: ${{ matrix.artifact }}
          if-no-files-found: warn

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    name: Publish Release

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-files
          merge-multiple: true

      - name: List release files
        run: ls -la release-files/

      - name: Get version from tag
        id: version
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

      - name: Read changelog entry
        id: changelog
        run: |
          VERSION="${{ steps.version.outputs.VERSION }}"
          NOTES=$(awk "/^## \[$VERSION\]/{flag=1;next}/^## \[/{flag=0}flag" CHANGELOG.md)
          echo "NOTES<<EOF" >> $GITHUB_OUTPUT
          echo "$NOTES" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ github.ref_name }}
          name: HexBrief v${{ steps.version.outputs.VERSION }}
          body: |
            ## HexBrief v${{ steps.version.outputs.VERSION }}

            ${{ steps.changelog.outputs.NOTES }}

            ---

            ### Download

            | Platform | File | Notes |
            |---|---|---|
            | Windows | `HexBrief-Setup-${{ steps.version.outputs.VERSION }}-x64.exe` | NSIS installer, recommended |
            | Windows | `HexBrief-Setup-${{ steps.version.outputs.VERSION }}-ia32.exe` | 32-bit installer |
            | macOS | `HexBrief-${{ steps.version.outputs.VERSION }}-x64-mac.zip` | Intel Mac — unzip and run |
            | macOS | `HexBrief-${{ steps.version.outputs.VERSION }}-arm64-mac.zip` | Apple Silicon — unzip and run |
            | Linux | `HexBrief-${{ steps.version.outputs.VERSION }}-x64.AppImage` | Universal |
            | Linux | `HexBrief-${{ steps.version.outputs.VERSION }}-amd64.deb` | Debian / Ubuntu |

            > **macOS note:** Right-click → Open on first launch to bypass Gatekeeper until the app is code-signed.
            > **Linux note:** `chmod +x HexBrief-*.AppImage` before running.

          draft: ${{ github.event.inputs.draft == 'true' }}
          prerelease: false
          files: release-files/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
